defaults: &defaults
  host: 127.0.0.1
  port: 27017

development:
  <<: *defaults
  database: manybots_local_development

test:
  <<: *defaults
  database: manybots_local_test

# set these environment variables on your prod server
production:
  <<: *defaults
  database: manybots_local
  username: <%= ENV['MONGO_USERNAME'] %>
  password: <%= ENV['MONGO_PASSWORD'] %>
